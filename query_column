■조회 쿼리

①테이블조회 (10분)
table from=20181217131049 to=20181217132049  order=asc  ts_data, ts_data_m14b		( from, to 시간, 오름차순 정렬, table 지정 )
| fields _time, TIME_EX, MACHINENAME, UNITNAME, CARRIER, COMMANDID, COMMAND, OPERATION_NAME, MESSAGENAME, PROCESS, TRANSACTIONID, TEXT, THREAD, key, LEVEL, XML, SECSII, RESULTCODE	( 컬럼 선택 )
| search in (LEVEL , "WELL", "WARN", "ERROR", "FATAL" ) 	( LEVEL 별 select )
|  sort _time 		( time 정렬 )
| limit 0 1000		( 1 ~ 1000번째 까지) 
| eval No = seq() +0	( rowNum )

②인덱스 검색시 (1시간)
fulltext from=20181216191223 to=20181216201223 (LEVEL=="WELL" or LEVEL=="WARN" or LEVEL=="ERROR" or LEVEL=="FATAL")
 and ((CARRIER=="검색carrier명") or (TEXT=="검색carrier명")) from ts_data, ts_data_m14b	( 인덱스 조건검색, from, to 시간, table 지정 )
| fields _time, TIME_EX, MACHINENAME, UNITNAME, CARRIER, COMMANDID, COMMAND, OPERATION_NAME, MESSAGENAME, PROCESS, TRANSACTIONID, TEXT, THREAD, key, LEVEL, XML, SECSII, RESULTCODE 	( 컬럼 선택 )
|  sort _time 		( time 정렬 )
| limit 0 1000 		( 1 ~ 1000번째 까지) 
| eval No = seq() +0	( rowNum )

■스트림
① 전체
①-1 ts_raw -> ts_data
Column: line -> _time(Date), AREANAME, BAYNAME, CARRIER, COMMAND, DESTINATION_ID, DESTINATION_TYPE,DESTMACHINENAME,DESTTYPE,DESTUNITNAME, ELAPSED, LEVEL, MACHINENAME, MESSAGENAME, OPERATION_NAME, PROCESS, RESULTCODE, SECSII, SOURCEMACHINENAME,SOURCEUNITNAME, SOURCE_ID, SOURCE_TYPE,START_TIME(Date), TEXT,THREAD, TIME_EX, TRANSACTIONID, TRANSPORTCOMMANDID, TYPE, UNITNAME, XML, method, strLength(Int)

①-2 ts-data -> ts_job_completed
Column: _time(Date), CARRIER, START_TIME(Date), ELAPSED, ELAPSED_MSEC(Int), DETAIL_LOCATION_ID, LOCATION_ID, REQUESTOR, method, TIME_EX, SOURCE_ID, DESTINATION_ID

② HISTORY 만 따로 parsing
②-1 ts_raw -> ts_alarm
Column: TIME_EX, _time(Date), UNITNAME, ALARMID, ALARMCODE, ALARMTEXT, STATE, MACHINENAME, AREANAME, BAYNAME, TYPE, method

②-2 ts_raw -> ts_material
Column: TIME_EX, _time(Date), CARRIER, LOTID, COMMANDID, UNITNAME, MACHINENAME, AREANAME, BAYNAME, TYPE, method, CURRENTMACHINENAME, CURRENTUNITNAME, TRANSPORTCOMMANDID

②-3 ts_raw -> ts_resource
Column: MACHINENAME, CRANENAME, STATE, SUBSTATE, PROCESSINGSTATE, TRANSPORTCOMMANDID, CONNECTIONSTATE, TSCSTATE, PORTNAME, BANNED, OCCUPIED, TRANSPORTUNITACCESSIBLE, CRANEAVAILABLE, INOUTTYPE, MANUAL, ACCESSMODE, IDREADSTATE, SHELFNAME, FULLSTATE, VEHICLENAME, CARRIER, TRANSFERPORTNAME, TIME_EX, _time(Date), method, MESSAGENAME, CONTROLSTATE

②-4 ts_raw -> ts_transport
Column: CARRIER, _time(Date), DESTMACHINENAME, DESTUNITNAME, DESTTYPE, method, SOURCEMACHINENAME, TIME_EX, TRANS_JOBSTART, TRANS_JOBEND,
SOURCEUNITNAME, STATE, STEPID, TRANSPORTJOBID, TRANSPORTCOMMANDID, LOTID, FIXEDROUTE, PRIORITY, PROCESSID, BATCHID, DESCRIPTION, REASON,
SOURCEAREANAME, SOURCEBAYNAME, SOURCEMACHINETYPE, DESTAREANAME, DESTBAYNAME, DESTMACHINETYPE, SOURCEUNITNAME, CREATEUSER, BATCHTYPE, TRANSPORTMACHINENAME, TRANSPORTUNITNAME

